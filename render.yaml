services:
  - type: web
    name: motionfalcon-backend
    env: python
    rootDir: crypto-backend
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python fix_migrations.py"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: crypto-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "https://crypto-frontend-lffc.onrender.com,http://localhost:3000,http://localhost:5173"
      - key: ENVIRONMENT
        value: "production"
      - key: RENDER
        value: "true"
      - key: FORCE_REDEPLOY
        value: "2024-01-01-$(date +%s)"
      - key: REDEPLOY_TRIGGER
        value: "migration-fix-v2"

  - type: static
    name: crypto-frontend
    rootDir: crypto-frontend
    region: oregon
    buildCommand: "npm install && npm run build"
    publishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: "https://motionfalcon-backend.onrender.com"



databases:
  - name: crypto-db
    plan: free
    region: oregon

  